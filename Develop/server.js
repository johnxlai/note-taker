const express = require('express');

const PORT = 3001;
const app = express();

//app use

//Get route

// app listen

app.listen(PORT, () =>
  console.log(`App listening at http://localhost:${PORT}`)
);
